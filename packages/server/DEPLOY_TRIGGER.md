# Deploy trigger

Marker file to re-trigger the Firebase App Hosting server build after an
out-of-band infrastructure change (Cloud Secret Manager IAM grant on
`CHROMA_CLOUD_API_KEY`). No code change required; deleting this file is safe.
