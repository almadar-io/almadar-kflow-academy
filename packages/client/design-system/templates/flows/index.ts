/**
 * Learner-journey flows — persona-facing page templates that compose other
 * templates/organisms into a multi-step learner workspace (mirrors
 * apps/builder `design-system/templates/flows/`). Populated during the
 * implementation waves (e.g. LearnerJourneyFlow, GraphExplorerFlow,
 * StoryExplorerFlow).
 */
export {};
